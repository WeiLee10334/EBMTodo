@model IEnumerable<EBMTodo.Models.Todo.EBMProjectMember>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EBMProject.ProjectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreateDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationUser.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EBMProject.ProjectName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PMID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PMID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PMID })
        </td>
    </tr>
}

</table>
