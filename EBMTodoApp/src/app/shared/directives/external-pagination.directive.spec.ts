import { ExternalPaginationDirective } from './external-pagination.directive';

describe('ExternalPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
