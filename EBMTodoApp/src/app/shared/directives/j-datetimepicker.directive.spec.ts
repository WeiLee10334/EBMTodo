import { JDatetimepickerDirective } from './j-datetimepicker.directive';

describe('JDatetimepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new JDatetimepickerDirective();
    expect(directive).toBeTruthy();
  });
});
